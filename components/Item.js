import React from 'react';
import {View, StyleSheet } from 'react-native';
import PropTypes from 'prop-types';

const Item = ({id, name, onDelete}) => {
  return (
    <View>
       {/* 
        // TODO: Add All necessary components to have a beautifull Food List Item with image, Icons and text 
       */}
    </View>
  )
};

const styles = StyleSheet.create({
  // TODO: Add your custom styles
});

Item.propTypes = {
  // TODO: 
}

export default Item;
